{
  "name": "node-chip8",
  "version": "0.0.0",
  "description": "chip8 node binding",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "tap test.js --cov"
  },
  "gypfile": true,
  "dependencies": {
    "bindings": "^1.3.1",
    "node-addon-api": "^1.6.2"
  },
  "devDependencies": {
    "tap": "^12.1.1"
  }
}
